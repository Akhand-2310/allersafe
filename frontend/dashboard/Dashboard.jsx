import { Camera, Edit2, MessageSquare, ShieldAlert, ShieldCheck } from 'lucide-react';
export function Dashboard({ userProfile, setRoute }) { return <div className="page-shell space-y-8"><div><h1 className="welcome-title text-4xl font-black mb-2">Welcome back, {userProfile?.name}</h1><p className="welcome-subtitle text-lg">Your workspace is ready. What would you like to analyze today?</p></div><div className="grid lg:grid-cols-3 gap-6"><section className="panel"><div className="flex justify-between items-center border-b pb-4 mb-6"><h2 className="section-title"><ShieldAlert className="text-rose-500" /> Active Watchlist</h2><button onClick={() => setRoute('settings')} className="icon-button"><Edit2 /></button></div>{userProfile?.allergens?.length ? <div className="flex flex-wrap gap-3">{userProfile.allergens.map(item => <span className="allergen-pill" key={item}>{item}</span>)}</div> : <p className="text-slate-500 italic">No allergens being tracked.</p>}<div className="status-box"><ShieldCheck /><div><h4>System Active</h4><p>AI filtering is armed and ready.</p></div></div></section><div className="lg:col-span-2 grid sm:grid-cols-2 gap-6"><button onClick={() => setRoute('scan')} className="action-tile dark"><Camera /><strong>Scan Menu</strong><span>Extract text from images</span></button><button onClick={() => setRoute('chat')} className="action-tile blue"><MessageSquare /><strong>Ask AI Chat</strong><span>Get instant ingredient advice</span></button></div></div></div>; }
